一个swift的macos桌面软件开发。
请使用中文和我交流。
我的界面要求：
1. 界面上包含一个图片显示区域，用来展示用户截图后的图片
2. 一个转换按钮
3. 图片OCR后，一个LaTeX公式输出的区域

功能要求：
1. 检测用户使用了command+P的快捷键，此时调用截图工具。
2. 用户截图成功，将该截图读取别显示到该图片区域
3. 若用户退出截图，则无返回

以上功能目前的实现非常好。
我希望有以下的新功能。
1. 我希望我关闭window后，dock栏不显示应用图标。
2. 菜单栏始终显示应用图标，当点击菜单栏图标时，弹出应用的window